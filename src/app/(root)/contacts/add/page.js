import AddContactPage from '@/components/contacts/pageContainers/AddContactPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <AddContactPage />
        </div>
    );
};

export default page;